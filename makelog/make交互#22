
**AI 开发指令（基于 v2.6.0 继续开发）**

---

### 一、项目现状（请先阅读）

本项目是一个 **刷题与考试**，当前版本为 **v2.6.0**。程序的核心架构如下：

#### 1. 主窗口布局
- **左侧侧边栏**：可收缩/展开，包含功能按钮（刷题、考试、题库管理、导入、导出、统计、错题本、退出）。
- **右侧内容区域**（`_content_area`）：根据侧边栏点击切换显示不同面板。
- **单窗口面板切换**：所有功能均在主窗口内切换显示（不再弹 Toplevel），通过 `_show_panel()` / `_show_main_menu()` 实现。

#### 2. 已有功能模块
- **多题库管理**（`BankManager` + `banks/` 目录）：
  - 题库存储在 `banks/` 文件夹，每个题库独立的 JSON + 历史文件。
  - 注册表 `banks/index.json` 记录所有题库及当前活跃题库。
  - 支持创建、切换、重命名、删除题库。
  - 支持外部题库文件添加（任意路径）和文件夹扫描导入。
- **刷题模式（练习）**：
  - 随机打乱题目，一次显示一道。
  - 支持左右箭头切换、`A/B/C/D` 快捷键选择、回车提交。
  - 显示/隐藏答案功能。
  - 错题自动记录到错题本（`banks/<题库名>_wrong.json`）。
- **考试模式**：
  - 按题型分别设置题量（单选/多选/填空各设数量）。
  - 限时倒计时，时间到自动提交。
  - 禁看答案，提交后显示成绩并保存历史。
- **题库管理**：Treeview 显示题目列表，支持增删改查、搜索、双击编辑。
- **TXT 导入/导出**：支持从文件或粘贴文本导入，按规范格式解析；导出为 TXT 文件。
- **统计**：题库统计 + 历史成绩记录（`history.json`）。
- **错题本**：
  - 嵌入主窗口的面板（`_content_area` 切换）。
  - 显示错题列表（题目预览、题型、错误次数，按次数排序）。
  - 支持多选删除、清空错题本、刷错题。

#### 3. 技术栈
- Python 3.x + Tkinter（标准库）。
- 零第三方依赖（`requirements.txt` 为空）。
- 数据存储：JSON 格式。

#### 4. 关键类结构
| 类 | 职责 |
|----|------|
| `Question` (ABC) | 题目基类 |
| `SingleChoiceQuestion` | 单选题 |
| `MultiChoiceQuestion` | 多选题 |
| `FillInBlankQuestion` | 填空题 |
| `QuestionBank` | 题库逻辑（加载/保存/增删改查） |
| `BankManager` | 多题库管理（切换/创建/删除） |
| `MainApp` | 主窗口（侧边栏 + 内容切换） |
| `PracticePanel` | 刷题面板（嵌入 `_content_area`） |
| `ExamPanel` | 考试面板（嵌入 `_content_area`） |
| `WrongBookPanel` | 错题本面板（嵌入 `_content_area`） |
| 其他管理窗口 | 题库管理、导入导出、统计等（独立 Toplevel） |

---

### 二、新需求说明

**请在当前 v2.6.0 代码基础上，实现以下新功能：**

#### 1. 题库管理窗口增强
- **题库管理窗口**（`BankManagementWindow`）当前是 Toplevel，请新增以下功能：
  - **题库重命名**：右键或双击题库名称可编辑重命名（更新 `index.json` 和文件夹名称）。
  - **题库复制**：支持复制一个完整题库（包括所有题目和历史错题记录），生成副本，名称自动添加 `_副本` 后缀。
  - **题库导出**：选中的题库可导出为独立 JSON 文件（用户选择保存路径）。

#### 2. 刷题模式增强
- **刷题进度持久化**：
  - 每次进入刷题模式时，记录当前答题进度（已答题目索引、答案、对错状态）。
  - 退出刷题时自动保存进度到 `banks/<题库名>_progress.json`。
  - 再次进入同一题库刷题时，弹窗询问“是否继续上次进度？”。
  - 支持“重新开始”重置进度。
- **随机模式与顺序模式切换**：
  - 刷题面板新增切换按钮（“随机模式” / “顺序模式”）。
  - 随机模式：题目随机打乱（现有行为）。
  - 顺序模式：按题库原始顺序逐题显示，不随机。
  - 模式切换时重置当前进度并提示。

#### 3. 考试模式增强
- **考试结果详情**：
  - 考试结束后，成绩窗口增加“查看详情”按钮。
  - 点击后显示每道题的作答情况（题号、你的答案、正确答案、对/错）。
  - 支持将详情导出为 TXT 文件。
- **防作弊**：
  - 考试期间禁用 Alt+Tab / Win 键（全屏锁定）—— 如实现难度大，可简化：考试窗口置顶并捕获焦点丢失事件，退出时自动提交试卷。

#### 4. 统计面板增强
- **统计面板新增图表**（使用 Tkinter Canvas 绘制简单条形图）：
  - 各题型正确率条形图。
  - 历史考试正确率趋势图（折线图，按时间排序）。
- **统计面板新增导出**：
  - “导出统计报告”按钮，将当前统计数据导出为 TXT 或 CSV 文件。

#### 5. 全局配置
- **新增 `config.json`** 存储用户偏好设置：
  - `practice_show_answer`：刷题时是否默认显示答案（布尔值）。
  - `practice_mode`：默认刷题模式（`random` / `sequential`）。
  - `exam_default_duration`：考试默认时长（分钟）。
  - `theme`：浅色/深色主题（预留，暂不实现）。
- **主菜单新增“设置”按钮**（齿轮图标），点击弹出设置窗口，可修改上述配置项。

#### 6. 性能与体验优化
- **大题库加载优化**：当题库超过 500 题时，Treeview 采用分页加载（每页 50 条）。
- **键盘快捷键统一**：
  - `Ctrl+1` ~ `Ctrl+9` 快速切换到对应侧边栏菜单项（按顺序）。
  - `Ctrl+W` 关闭当前子面板返回主菜单。
- **托盘图标**（可选）：最小化时缩到系统托盘（需 `pystray` 库，如实现困难可暂不包含）。

---

### 三、代码要求

1. **保持现有架构**：新功能应集成到现有的类结构中，不破坏已有功能。
2. **面板化**：所有新增功能应优先嵌入主窗口 `_content_area`，避免过多的 Toplevel 弹窗。
3. **向后兼容**：旧版题库 JSON 和历史记录格式应能正常加载。
4. **异常处理**：所有文件读写、用户输入均需 try-except，程序不应崩溃。
5. **注释**：关键逻辑和新增部分需添加中文注释。
6. **零依赖**：继续坚持纯标准库，除非必须使用第三方库（如 `pystray`），则应在 `requirements.txt` 中注明并给出安装说明。

---

### 四、交付内容

请生成更新后的完整代码，并以下列方式组织：

1. 修改后的 `main.py`（或拆分为多个模块，如 `panels.py`、`widgets.py` 等）。
2. 更新 `CHANGELOG.md`，记录本次新增功能。
3. 更新 `README.md` 或代码注释中的使用说明。
4. 如新增了第三方依赖，更新 `requirements.txt`。

---

**请基于上述需求，生成完整的 Python 代码，确保功能完整、界面流畅、代码规范。**