

**AI 开发指令（v2.8.0 布局重构）**

---

### 一、项目现状（请先阅读）

当前程序版本为 **v2.7.0**，已完成：
- 全局配置系统（`config.json`）
- 刷题进度持久化 + 随机/顺序切换
- 考试逐题详情 + 导出 + 防作弊（失焦警告）
- 题库复制/导出
- 统计图表（条形图 + 折线图）+ 报告导出
- 侧边栏可收缩（左侧） + 右侧内容区的单窗口架构

完整功能清单详见 `CHANGELOG.md`（v2.7.0）。

---

### 二、新需求：UI 布局全面重构

请基于 v2.7.0 代码，对主窗口布局进行彻底改造，实现以下目标：

#### 1. 新布局结构

- **左侧竖向菜单栏**（原侧边栏）：
  - 宽度可收缩（保留 `«` / `»` 切换），默认展开。
  - 竖向排列功能按钮，从上到下依次为：
    1. **刷题模式**（图标 + 文字，字体放大，视觉突出）
    2. **考试模式**（图标 + 文字，字体放大）
    3. **错题本**（图标 + 文字，字体放大）
    4. **题库管理**（图标 + 文字，合并原“题库管理”、“导入”、“导出”、“切换题库”为一体）
  - **统计**按钮：单独放置在 **菜单栏左上角**（标题区域或菜单最顶部），**只显示图标**（如 📊），不显示文字。
  - **退出**按钮：单独放置在 **菜单栏右下角**（底部），**只显示图标**（如 ⏻ 或 ✕），不显示文字。
- **右侧内容区域**：
  - 根据左侧菜单点击，切换显示对应的功能面板（刷题/考试/错题本/题库管理）。
  - 面板内容需完整实现原有功能（逻辑不变，仅入口整合）。
  - 统计功能点击后可在内容区显示统计面板（或弹窗，但优先内容区）。

- **取消“设置”按钮**：
  - 全局设置（默认刷题模式、是否显示答案、默认考试时长）不再独立入口。
  - 将设置选项整合到 **刷题模式面板** 中（例如在刷题界面顶部增加“设置”齿轮图标或下拉菜单，修改后立即生效并保存到 `config.json`）。

#### 2. 紧凑模式适配

- 当窗口面积小于屏幕 1/3 时，左侧菜单栏进一步缩小（只显示图标，文字隐藏，悬停显示提示）。
- 右侧内容区仍正常显示功能，字体和控件大小自动缩小（保持原有紧凑逻辑）。

#### 3. 题库管理面板合并

- 点击“题库管理”后，右侧内容区显示一个统一面板，包含：
  - **题库列表**（显示所有题库，支持切换、重命名、复制、导出JSON）
  - **题目管理**（当前题库的题目列表、增删改查、搜索）
  - **导入/导出**（TXT 导入导出、格式说明）
- 所有子功能放在同一面板的不同区域或分页（可使用 Notebook 或 Frame 切换）。

#### 4. 统计面板

- 点击统计图标后，右侧内容区显示统计面板（包含原有统计图表和历史记录）。
- 统计面板顶部保留“导出统计报告”按钮。

#### 5. 其他细节

- 左侧菜单按钮文字和图标应清晰对应功能。
- 刷题、考试、错题本三个主要功能按钮在视觉上可稍大（如增大字体和图标大小）。
- 所有键盘快捷键（Ctrl+1~8 等）需要重新映射到新的按钮顺序（Ctrl+1 对应刷题，Ctrl+2 考试，Ctrl+3 错题本，Ctrl+4 题库管理，Ctrl+5 统计，Ctrl+6 退出等），注意统计和退出只有图标，快捷键仍可用。
- `Ctrl+W` 返回主菜单（即清空内容区显示默认欢迎页或回到刷题面板？建议点击刷题默认进入刷题，但“返回主菜单”可以改为回到默认显示刷题面板）。

---

### 三、代码要求

1. **保持现有功能逻辑完全不变**，仅重构 UI 布局和入口。
2. **架构调整**：
   - 修改 `MainApp` 类，重构侧边栏创建和内容切换。
   - 将原有独立 Toplevel 窗口（如题库管理、导入导出、统计）的内容迁移到内容区域的面板中（如 `BankManagementPanel`, `StatisticsPanel`），尽量不再弹窗。
   - 刷题、考试、错题本已经是面板，无需改动，只需调整显示方式。
3. **紧凑布局适配**：在 `_update_layout` 方法中增加对左侧菜单栏的响应式调整。
4. **异常处理**：所有文件读写、配置保存需加 try-except。
5. **注释**：新增或修改的关键代码需添加中文注释。

---

### 四、交付内容

- 重构后的 `main.py`（或拆分为 `ui_panels.py` 等模块）。
- 更新 `CHANGELOG.md`，记录 v2.8.0 的变更。
- 更新使用说明（README 或注释）。

---

**请基于上述需求，生成完整的 Python 代码，实现全新布局，确保所有功能正常可用。**