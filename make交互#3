

**AI 生成指令（GUI + 考试模式版）：**

请用 Python 编写一个**图形界面（GUI）刷题与考试程序**，使用 **Tkinter**（Python 内置库）作为 GUI 框架，要求支持**单选题、多选题、填空题（含多空、多答案匹配）**，并具备**题库管理、TXT 导入/导出、交互式刷题、考试模式（限时、禁看答案）、统计**等功能。

程序应在一个独立的窗口中运行（非命令行），所有交互通过窗口控件（按钮、标签、文本框等）完成。请确保程序无需额外安装第三方库（Tkinter 为 Python 标准库），但若使用了其他第三方库，请在根目录生成 `requirements.txt` 并注明。

---

### 一、核心功能模块

#### 1. 主窗口（主菜单）
- 程序启动后显示主窗口，包含以下功能按钮（或菜单）：
  - **刷题模式**（练习）
  - **考试模式**
  - **题库管理**（增删改查）
  - **导入题库**（从 TXT 文件或粘贴文本）
  - **导出题库**（到 TXT 文件）
  - **查看统计**（题库统计 + 历史/本次成绩）
  - **退出程序**

#### 2. 刷题模式（练习）
- 随机打乱所有题目，**一次只显示一道题**。
- 界面控件：
  - 题目标题区（显示题号、题型、题目内容）。
  - 选项区（单选题用 Radiobutton，多选题用 Checkbutton，填空题用 Entry 输入框）。
  - **“上一题”** 和 **“下一题”** 按钮（或键盘左右键支持，在 GUI 中优先使用按钮）。
  - **“显示/隐藏答案”** 按钮（默认隐藏，点击后显示当前题的正确答案）。
  - 提交答案按钮（点击后判断对错并记录，但不清除答案）。
- 显示进度（如“第 3/20 题”）。
- 刷题完成后（所有题答完）自动弹出成绩统计窗口。

#### 3. 考试模式（新增）
- **设置界面**：
  - 从当前题库中选择（或默认全部）题目。
  - 设置**试卷题量**（N 道题，若题库总题量不足则提示）。
  - 设置**考试时长**（分钟，例如 30 分钟）。
  - 点击“开始考试”进入考试。
- **考试进行中**：
  - 随机抽取 N 道题作为试卷（不重复）。
  - 全屏或最大化窗口，**隐藏所有查看答案的功能**（如“显示答案”按钮不可用或隐藏）。
  - 顶部显示倒计时（分钟:秒），时间到自动提交试卷。
  - 显示当前题号（如“第 5/10 题”）。
  - 支持“上一题/下一题”切换，但**答题后不能修改答案**（或允许修改，取决于设计，建议允许在提交前修改）。
  - **提交按钮**：用户可提前提交，提交后显示成绩。
- **考试成绩**：
  - 显示正确题数、正确率、各题型正确率。
  - 保存本次考试记录（用于统计历史成绩，可存储在本地 JSON 文件中）。

#### 4. 题库管理（GUI）
- **显示所有题目**：在一个滚动列表或 Treeview 中显示全部题目（序号、题型、题目预览）。
- **添加题目**：弹出窗口，选择题型，输入题目、选项、答案，确认后添加。
- **删除题目**：选中列表中的题目，点击删除按钮（需二次确认）。
- **修改题目**：选中后弹出编辑窗口，可修改任意字段。
- **查找题目**：提供搜索框，根据关键词实时筛选显示题目。

#### 5. 导入导出（TXT）
- **导入**：弹窗让用户选择导入方式（从文件选择或粘贴文本），解析后追加到题库（若格式错误给出提示）。
- **导出**：弹出保存文件对话框，将当前全部题目按规范格式导出为 TXT 文件。

#### 6. 统计功能
- **题库统计**：显示各类题型数量。
- **成绩统计**：显示所有历史考试记录（日期、试卷题量、得分、用时等），并计算平均正确率。

---

### 二、技术细节要求

1. **界面布局**：使用 Tkinter 的 Frame、Label、Button、Entry、Text、Radiobutton、Checkbutton、Treeview（可选）等组件，界面整洁美观。
2. **数据持久化**：
   - 题库使用 JSON 文件存储（与之前格式一致）。
   - 成绩记录可另存为 JSON 文件（如 `history.json`），记录每次考试的时间、题量、正确数、用时等。
3. **异常处理**：对所有用户操作（文件读写、格式解析、输入验证）进行 try-except 处理，避免程序崩溃。
4. **代码结构**：采用面向对象设计，至少包含以下类：
   - `Question`（题目对象）
   - `QuestionBank`（题库管理）
   - `ExamEngine`（考试逻辑）
   - `MainApp`（主窗口）
   - `ExamWindow`（考试窗口）
   - `ResultWindow`（成绩显示窗口）
5. **环境依赖**：仅依赖 Python 标准库（tkinter、json、random、os、datetime 等），**无需第三方包**，因此 `requirements.txt` 可为空或注释说明。

---

### 三、使用说明（内置在代码注释中或提供 README）

- 运行 `main.py` 启动程序。
- 首次使用需通过“导入”功能添加题目，或手动在题库管理中添加。
- 在刷题模式中可自由练习，查看答案。
- 在考试模式中必须完成所有题目方可提交，时间到自动交卷。
- 所有数据保存在程序所在目录下的 `questions.json` 和 `history.json` 文件中。

---

### 四、扩展性提示

- 预留接口以便未来增加判断题、连线题等新题型。
- 可考虑支持选择题选项数量动态变化（不限于 4 个选项）。

---

**请生成完整的 Python 代码，实现上述所有功能，确保界面友好、逻辑完整，可直接运行。** 代码中应包含必要的注释，说明关键部分。